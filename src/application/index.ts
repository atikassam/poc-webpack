const $ = require("jquery");

// Lets change the text of poc container
$('#poc').html("This is a simple poc for webpack")
$('#sub').html("Webpack is successfully bundled all the dependences in one static file in main.js")